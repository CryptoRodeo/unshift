Bug:
- commit and pull request does not get created after approval
- Jira issue does not get moved to next phase after work is done

UX:
- I see two sections that say "Implemenation plan".
- The text section under "Approval Required" does not take into account the theme. The background and text are both white.
- The "Approve & Create PR" button did nothing, no PR was created.
- The Run ID should be the Jira issue ID, not a random UUID.
