{
  "name": "unshift-dashboard",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build -w client && npm run build -w server",
    "start": "npm run start -w server",
    "start:dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\""
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "js-yaml": "^4.1.1"
  }
}
